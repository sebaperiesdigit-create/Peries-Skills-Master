# 2026-08-13

Today's session, in progress — written live, not reconstructed. Commits so far: `239c810`, `0d02030`, `31daa2a`, `5cab54c`, `105def1`, plus an uncommitted `SKILL.md` improvement round and catalog re-sync as of this writing.

## New skill: `new-joiner-guide` (`239c810`)

User added a new skill folder, `.claude/skills/new-joiner-guide/SKILL.md`. Analysis and decision-making before building anything:

- **Overlap with `start`/`new-joinee`**: both cover "I am new" / "how do I start" triggers. Resolved as deliberately **coexisting, not connected** — `start`/`new-joinee` stay generic/role-agnostic; `new-joiner-guide` is Mini-AIOS-specific and operational. Description narrowed to lead with "Mini-AIOS" and add distinguishing terms so it doesn't collide with the other two's generic triggers. No cross-links added.
- **Real names/DB details** (Varmens, Sajeesan, Tamil Selvan, `varmensk.digitweb@gmail.com`, table naming patterns) are intentional and supervisor-approved for publication — not fabricated, not redacted.
- **Source PDF**: user supplied `Mini-AIOS_New_Joiner_Complete_Guide (1) (1).pdf` (messy download-artifact filename) in a `reference/` (singular) folder. Renamed to `.claude/skills/new-joiner-guide/references/mini-aios-new-joiner-complete-guide.pdf`, matching the `references/` (plural) convention `mcp-access-guide` already uses.
- User then supplied the source `.docx` too. Converted it to `references/mini-aios-new-joiner-complete-guide.md` via `python-docx` (preserves headings/tables), validated against the `.pdf` via `pypdf` text extraction (all 16 pages, all 14 section headings, 17 salient facts cross-checked present in both) before trusting it. `SKILL.md`'s Source of Truth section points at this `.md` for runtime re-reading in Claude Code; the `.pdf`/`.docx` stay as source-of-record, never embedded anywhere (binary, can't be text-embedded).
- `CLAUDE.md`'s skill inventory table and folder count (24→25) updated.

## Catalog sync as skill 025 (`239c810`, review-notes §58)

Full "update skill docs" procedure: 12-field row added to `skill-data`/CSV, `skill-files-data['025']` and `skill-install-data['025']` added (install-data bundles `SKILL.md` + the `.md` reference only — binaries excluded, disclosed in `guideContent`), group `Getting Started`, `tryPhrase` derived mechanically. **Caught and fixed a real bug**: a stray curly apostrophe (’) introduced in a first draft — this CSV had zero curly apostrophes before the edit; caught by the standing quote-codepoint check, fixed before finalizing.

**Also caught while committing**: `.gitattributes` had a blanket `* text eol=lf` with no binary exclusion, silently corrupting the new `.pdf`/`.docx` on `git add` (staged blob didn't hash-match disk). Fixed by adding `*.pdf binary` / `*.docx binary` rules — see `project_gitattributes_binary_corruption_gotcha`. First-ever binary files in this repo; any future binary type needs the same treatment before staging.

Pushed to hub same session (verified via live page fetch, not just DB md5 match).

## Chat/Cowork install path (`0d02030`, review-notes §59)

User checked the live install-prompt feature and asked whether it covered Claude Chat/Cowork. It didn't — the shared `buildInstallPrompt()` used by all 25 skills is hardcoded Claude-Code-only (writes files to `.claude/skills/<name>/`). Added a **second button**, scoped only to `new-joiner-guide`'s row: a new `skill-chat-install-data` JSON block + an optional `mode` param on `openInstallModal()` (backward compatible — existing 2-arg calls unaffected). The prompt embeds `SKILL.md` **and** the full reference guide inline, paste-into-conversation.

## Save-gate fix (`5cab54c`, review-notes §60)

User tested the §59 prompt live in Cowork — it started coaching immediately with no save step. Real bug, not a false alarm. Fixed: prompt now has a hard-stop "BEFORE YOU DO ANYTHING ELSE — SAVE THIS SKILL FIRST" section (output a clean `SKILL.md` block, use a real save/register tool if the platform offers one or explain the manual step if not, then wait for explicit confirmation).

**Tested** via two fresh-context subagent runs (since no browser tool exists to literally test in claude.ai) — the full-fidelity run reproduced `SKILL.md` byte-for-byte (diffed and confirmed exact match), correctly reported no real save tool exists, and explicitly stopped and waited rather than proceeding into coaching.

## Six `SKILL.md` behavior fixes (uncommitted as of this writing, review-notes §61)

User ran the skill live in Cowork again and got (self-admitted) improvement feedback. Interrogated for scope before touching anything, all approved:

1. Reference A's connector bullet forbids inventing UI/menu paths (the skill had an existing rule against this and violated it anyway — this is the sharpest fix, a confirmed violation, not a hypothetical gap).
2. Two new Safety Guardrails bullets: the UI-fabrication ban reinforced, plus a universal "name Varmens first, personal opinion only as a separately labeled sentence, never blended" rule.
3. Step 7 gained contradiction detection (flag conflicts with anything already confirmed, anywhere in the session).
4. Step 8 now requires visible progress per substantive step — real `TaskCreate`/`TaskUpdate` where available, prose `Progress:` line as fallback.
5. Step 4 gained a silent-retry-once rule before falling back off `AskUserQuestion`.
6. New Reference J adds 4 escalation templates, explicitly headed "skill-added, not from the guide."

`SKILL.md` grew 275 → 305 lines (cap is 500). Guide files (`.pdf`/`.docx`/`.md`) never touched by any of this — everything lives in the operationalization layer only. Catalog re-synced to match (fileContent, install-data, chat prompt all refreshed; `Expected Output` field updated as the one genuine user-facing change) — **not yet committed or pushed to hub as of this writing**.

## This handover folder

Built this session, per explicit request — see `README.md` one level up for the living overview, and this folder's other dated files for everything before today (reconstructed from git log + the auto-memory system, not verbatim transcripts). Also corrected a stale memory found while reconstructing history: `project_sandbox_test_findings_2026-07-29` had 2 of 4 findings marked unfixed that were actually resolved on `2026-08-03`.
