# 2026-07-23

Reconstructed from memory only — no commits land on this exact date in git history (see `2026-07-27` for when this period's work was formally committed; `project_skills_master_structure` notes several skills' on-disk existence predates their git commits).

- `skill-finder` added — the 14th skill at that point. Read-only, chat-only skill that compares a proposed task against the skill catalog and recommends USE EXISTING / EXTEND / COMBINE / CREATE NEW / ESCALATE.
