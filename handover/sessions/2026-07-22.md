# 2026-07-22

Reconstructed from git history + memory (`feedback_rename_skill_files`, `project_skills_master_structure`) — no full transcript available for this or any date before 2026-08-13.

- **Initial commit** (`7690fd4`) and **"Folder setup & Implementation"** (`89b1af8`) — the repo's git history begins here, though several skills' own `Created Date` metadata (e.g. `start`, `new-joinee` both `2026/07/17`) predates the first commit — they existed on disk before git tracking started, with no git history available to confirm exact original dates precisely.
- At this point the repo used an older flat layout: `.claude/skills/skill-files/SKILL_<name>.md` and `.claude/skills/references/<purpose>_<skill>.md`, sibling files disambiguated by filename rather than Claude Code's real per-folder discovery convention. This meant skills were **not actually auto-discoverable** by Claude Code at this stage.
- A "rename" pass (per `feedback_rename_skill_files`) found `.claude/skills/skill-files/SKILL.md` had frontmatter `name: product-description-writer` but a generic filename inconsistent with its siblings — renamed to `SKILL_product_description_writer.md`. This established "rename" as a standing trigger word for a narrow filename-vs-frontmatter audit (not a full restructure).

**Not yet done at this point:** the move to real per-skill folders (`.claude/skills/<name>/SKILL.md`) — that came later; see `2026-07-27` onward, and `project_skills_master_structure` for the full before/after.
