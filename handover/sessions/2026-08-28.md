# 2026-08-28

Three new skills added, all `ecommerce-*` siblings, taking the catalog from 26 → 29. Two commits landed today (`7a9b928`, then `1cddd56` + `6f5d7e0`), on top of yesterday's `0be9cda` which had carried over uncommitted.

## New skill added: `ecommerce-geo-auditor` (27th skill, review-notes §69)

User added `.claude/skills/ecommerce-geo-auditor/` directly (single-file). Grilled three cross-cutting decisions before cataloguing: a reciprocal pointer back from `ecommerce-seo-auditor`, aligning the Output-format save step with the sibling's `AskUserQuestion` confirmation gate, and a `CLAUDE.md` inventory row. Also caught and backfilled a gap from §67: `ecommerce-seo-auditor` (026) had made it into `v5.html` but never into the actively-maintained CSV — fixed alongside adding 027. Placed in the existing "SEO & Marketing" group (no new group needed yet).

## New skill added: `ecommerce-sales-tracker` (28th skill, review-notes §70)

Single-file skill, read-only sales KPI dashboard/trend/anomaly/cohort analysis built only from user-supplied data — never a live platform connection. Same pre-catalogue grill pattern: added the missing `AskUserQuestion` save gate, added reciprocal back-pointers from `ecommerce-seo-auditor` and `ecommerce-geo-auditor` (they were missing the return link), added the `CLAUDE.md` row.

**Workflow group retired:** grilled whether 028 belonged in "SEO & Marketing" (audit-family) or needed its own group — user chose to create **"E-Commerce"** instead and move all three (`026`/`027`/`028`) into it, avoiding two overlapping ecommerce groups. The `SEO &amp; Marketing` dropdown option was replaced outright.

**New feature — 4-platform install picker:** user asked for separate install prompts (Claude Code / Claude Chat-Cowork / ChatGPT / Codex) on row 028 only, via one button opening a picker modal rather than 4 separate buttons. Established the content convention for the two platforms with no prior precedent: ChatGPT prompts are framed for a Custom GPT/Project's instructions field (flagged that regular per-account Custom Instructions likely can't fit the content); Codex prompts frame the save as a plain reference file, explicitly not claiming any auto-discovery mechanism, and ask the agent to confirm the destination path.

**Disclosed, not fixed this round:** while building 028's Claude Code file entry, found `skill-install-data` rows 021–024, 026, 027 were missing the `sha256` key their file objects need, so their generated Claude Code prompts rendered `sha256: undefined`. Logged as a known gap, deliberately not fixed in-scope (same precedent as an earlier disclosed CSV bug) — **fixed later, see 2026-08-31 below.**

## New skill added: `ecommerce-marketing-strategy-builder` (29th skill, review-notes §71)

The 4th `ecommerce-*` sibling and the **first multi-file one**: `SKILL.md` + 3 `references/` files (benchmarks, channel guidance, output template). Top finding: its Output-format step said to save "without asking permission first" — a real repo-wide safety-norm violation (every other skill gates saves behind `AskUserQuestion`), fixed before cataloguing. Also added optional back-references from the other three `ecommerce-*` skills, and kept it in "E-Commerce" rather than splitting a new "Strategy & Planning" group (user's explicit call, despite being planning- rather than audit-flavored). Install package for Claude Code includes all 4 files (SKILL.md + all 3 references), matching the precedent set by `daily-work-tracker` and `mcp-access-guide` for skills the workflow actually depends on.

## Retrofit: 4-platform picker extended to row 029 (review-notes §72)

Same day, user asked to extend the picker built for 028 to 029 as well — explicitly scoped to just that one row, not a full 29-row retrofit (still deferred). No HTML/JS structural changes needed (the picker mechanism is fully data-driven); the new part was adapting the Chat/Cowork, ChatGPT, and Codex prompts to carry all 3 reference files alongside SKILL.md, since none of those platforms have a filesystem to read them from — each got its own fenced block per file, with Codex additionally told to keep them in a `references/` subfolder so the skill's relative paths still resolve.

## Hub pushes today

Three: 05:07:21 UTC (§70, the 027/028 sync), 11:06:26 UTC (§71, the 029 add), 11:14:47 UTC (§72, the row-029 picker retrofit). All confirmed byte-identical live vs. local by sha256 after push.

## Git

`7a9b928` (geo-auditor + sales-tracker + catalog sync), `1cddd56` (marketing-strategy-builder + catalog sync), `6f5d7e0` (4-platform picker on row 029) — all three landed same day per the review-notes' running "not committed yet" notes being resolved by end of session.
