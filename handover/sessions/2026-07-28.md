# 2026-07-28

Commits: `363823b`, `f072a05`, `410ffb1`.

- **`363823b`** — added `daily-work-tracker` (the repo's first and still-only skill with executable code: three PowerShell helper scripts for marker/config state). Recorded its own first entry.
- **`f072a05` — the real CRLF bug fix.** The `2026-07-27` "stale browser tab" diagnosis was wrong: a real reproduction traced it to `.gitattributes` only having `* text=auto`, combined with this machine's `core.autocrlf=true`, silently injecting CRLF into 8 specific `SKILL.md` files on checkout. Any copy/paste path then normalized CRLF→LF, breaking every sha256 check for those 8 skills' install prompts 100% deterministically. Fixed by normalizing the 8 files to LF, adding `* text eol=lf` to `.gitattributes` (the durable part — prevents recurrence on any future clone regardless of local `autocrlf`), and hardening the install-prompt template to normalize line endings before hash comparison as defense-in-depth. Full detail: `project_v5_stale_tab_hash_gotcha`.
- **`410ffb1`** — logged the hub push covering the CRLF fix + `daily-work-tracker` sync (review-notes §36).
- The catalog's technical schema (3 JSON `<script>` blocks + a separate `skillGroups` JS mapping, `tryPhrase` derivation rule) was worked out and documented today while adding `daily-work-tracker` as the 20th skill — see `project_skill_catalog_new_skill_procedure`. This became the standing procedure for every later "update skill docs" sync.
