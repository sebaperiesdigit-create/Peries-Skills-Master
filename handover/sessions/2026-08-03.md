# 2026-08-03

By far the busiest day in the repo's history — roughly 30 commits. A systematic `skill-builder`-driven audit swept nearly every skill in the repo, each audit immediately followed by a catalog sync and (in batches) a hub push. Full itemized detail for each catalog sync lives in `output/skill-documentation/skill-documentation-review-notes.md` §41–§56; this file summarizes the pattern and the standalone events.

## The audit sweep

Skills audited today, roughly in order (`Audit <skill>: <headline fix>` commit, then `Sync <skill> audit into skill catalog`, then periodic `Log hub push of catalog section N`):

- `start` — restricted `allowed-tools`, grounded role examples in `company-workflow.md`.
- `new-joinee` — sequenced against `start` (soft-follow if already completed).
- `project-discovery` — fixed `allowed-tools` scoping, added `context: fork` — this is the fix that resolved sandbox finding #1 from `2026-07-29`.
- `existing-asset-finder` — removed an unused `Bash(git *)` grant.
- `first-task-mapper` — switched to `AskUserQuestion`, delegated overlap-checking.
- `requirements-validator` — wired up `$ARGUMENTS`, added `allowed-tools` and an output template.
- `markdown-document-formatter` — added a write-confirmation gate, wired up `$ARGUMENTS` (the code-fence fix-rule gap from sandbox finding #4 was **not** part of this audit — still open).
- `meeting-note-summariser` — added an `allowed-tools` restriction.
- `aios-structure-build` / `aios-structure-organize` / `aios-structure-validate` — each scoped `allowed-tools` to its exact script call.
- `record-a-skill` — documented two glob-scoping limitations.
- `daily-work-tracker` — wired up `$ARGUMENTS`, spelled out script calls explicitly, added `allowed-tools`.
- `task-closure` — scoped `Bash` to read-only git commands only.
- `mcp-access-guide` — added a 4th connector status label, "Installed — Not authenticated" — this is the fix that resolved sandbox finding #3 from `2026-07-29`.
- `grill-me` — decoupled it from this user's personal auto-memory system (it referenced `[[wiki-link]]`-style memory names that would be meaningless to anyone installing it standalone via the catalog), added `allowed-tools` and a defined output path.

## Standalone events, same day

- **CSV quote-corruption incident**: an `Edit` call meant to update `new-joinee`'s Notes field in the source CSV silently turned straight `"` delimiters into mismatched curly quotes, corrupting that row's structure — caught only by deliberately checking quote character codepoints afterward, not by the edit reporting failure. Root cause never conclusively identified. See `project_csv_quote_corruption_gotcha`. **As of 2026-08-13 this row (002) is still corrupted** — confirmed still present via a column-count check, never repaired.
- **`record-a-skill` → `record-a-skill-custom` rename requested, then postponed** by the user before execution (to preempt a possible collision with an official Anthropic skill of the same name). No files touched. Scoping work (7 affected files) is preserved in `project_record_a_skill_rename_postponed` if it's picked back up later.
- **The mandatory clickable-question convention was decided today** in a `grill-me` session (decision-vs-free-text boundary worked out), though the actual documentation/commit landed the next day — see `2026-08-04`.
